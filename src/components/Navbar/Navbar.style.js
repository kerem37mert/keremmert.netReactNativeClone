import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: "#3399cc",
        padding: 30,
        alignItems: "center"
    },
    title: {
        fontWeight: "500",
        fontSize: 32,
        color: "#fff"
    }
});3